<h4>画像投稿フォーム</h4>
<%= form_with model: @post_image, url:'/post_images/new', local:true do |f| %>
  <%= f.attachment_field :image %> 
  <h4>ショップ名</h4>
  <%= f.text_field :title %>
  <h4></h4>
  <%= f.text_area :body %>
  <!-- 追加部分 -->
  <h4>画像</h4>
<%= f.submit '投稿' %>
<% end %>
